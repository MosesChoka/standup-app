<!DOCTYPE html>
<html class="h-full">
  <head>
    <title>StandupApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="bg-gray-800 font-sans leading-normal tracking-normal mt-12 h-full">
    <%= render partial: 'layouts/components/header' %>
    <div class="flex flex-col md:flex-row min-h-full">
      <%= render partial: 'layouts/components/sidebar' %>
      <div class="main-content flex-1 bg-gray-100 mt-16 md:mt-2 pb-24 md:pb-5">
        <div class="flex items-center justify-center">
          <%= flash_messages %>
        </div>
        <%= yield %>
        <%= render partial: 'layouts/components/footer' %>
      </div>
    </div>
  </body>
</html>
